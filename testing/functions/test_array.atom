

Point:
    Int x
    Int y   

    void init(Int ix, Int iy):
        this.x = ix
        this.y = iy

    Int getX():
        return this.x
    
    Int getY():
        return this.y

Int main():
        "=== Basic array Test ===".println()

        Int[] arr = [1, 2, 3, 4, 5]
        "size=%i".println(arr.size())

        Bool[] barr = [1, 0, 1, 0, 1, true, false]
        "size=%i".println(barr.size())

        Real[] rarr = [1.1, 2.2, 3.3]
        "rsize=%i".println(rarr.size())

        Point[] parr = [Point.new(10, 20), Point.new(30, 40)]
        "psize=%i".println(parr.size())
        Point p0 = parr[0]
        "p0: %i %i".println(p0.getX(), p0.getY())

        return 0
