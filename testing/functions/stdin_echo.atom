# Test CLI argument passing in Atom

Int main(String[] args):
        Int argc = args.count()
        "Args: %i".println(argc)

        Int i = 0
        while (i < argc):
            "%i: %s".println(i, args.getString(i))
            i = i + 1
        return 0
